import { styled } from '#/stitches.config';

export const Container = styled('div', {
  width: '100vw',
  height: '100vh',
});

export const Map = styled('div', {
  width: '100%',
  height: '100%',
});
